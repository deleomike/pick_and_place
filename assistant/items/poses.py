
from spatialmath import SE3


# Define custom poses for the cubes
poses = [
    SE3(1, 0, 0),  # Pose for cube 1
    SE3(2, 1, 0),  # Pose for cube 2
    SE3(3, 2, 0),  # Pose for cube 3
    SE3(4, 3, 0),  # Pose for cube 4
    SE3(5, 4, 0),  # Pose for cube 5
]